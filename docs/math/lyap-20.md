# Lyapunov Stability Proof Notes

The discrete potential function  
`V(n) = (N(n) − N(n−1))²`  
is strictly decreasing when the harmonic step `ΔN` is bounded by `ε ≤ 0.0002185`.  
HV-221’s harness asserts that `ΔV < 0` for 20 000 iterations.
