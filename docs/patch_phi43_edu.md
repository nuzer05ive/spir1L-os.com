# Φ-Spir1L Edu-Forge MAX Patch

This patch augments the phi43 bloom spec with a Self-Spir1L curriculum and UI layer.

## Summary
- Adds YAML lessons under `content/priivi3/` including `self_spir1l.yaml`.
- Introduces lesson loader and education prompt hooks.
- Defines avatar mapping and LemonSqueezy payment split.
- Provides new front-end directories for VR reel forge and ops dashboard.

Developers apply with:
```bash
pnpm i
pnpm -F reel-forge-vr dev
```
